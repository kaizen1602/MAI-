# Variables de entorno globales para Docker Compose
# Copiar como .env y ajustar valores sensibles antes de levantar los contenedores
#
# cp .env.example .env

# Credenciales MySQL (usadas por el servicio "mysql" del docker-compose)
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_DATABASE=mai_db
MYSQL_USER=mai_user
MYSQL_PASSWORD=change_me_user

# Credenciales PhpMyAdmin
# Puedes usar root o el usuario de aplicación
PMA_USER=root
PMA_PASSWORD=change_me_root
